{
  "name": "nex-commerce",
  "version": "1.0.0",
  "description": "nex-commerce: an e-commerce website built to exhibit the skills of a full-stack developer",
  "scripts": {
    "develop": "concurrently \"cd backend && npm run watch\" \"cd frontend && npm run dev\" ",
    "seed": "cd backend && npm run seed"
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
